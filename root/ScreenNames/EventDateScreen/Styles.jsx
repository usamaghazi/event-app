import styled from 'styled-components/native'

export const Container = styled.View({
    flex:1,
    paddingTop:10,
    paddingHorizontal:25,
    backgroundColor:'#fff',
})

export const NoEvent = styled.View({
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    marginTop: 50
})

export const NoEventText = styled.View({
    fontSize: 16,
    color: '#666',
    fontStyle: 'italic',
})